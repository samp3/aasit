<%= form_for(@reftype) do |f| %>
  <% if @reftype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reftype.errors.count, "error") %> prohibited this reftype from being saved:</h2>

      <ul>
      <% @reftype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
